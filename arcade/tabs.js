(function(){'use strict'; const $=s=>document.querySelector(s); const =s=>Array.from(document.querySelectorAll(s)); function activate(name){ ('.tab').forEach(b=>{const on=b.dataset.tab===name; b.classList.toggle('active',on); b.setAttribute('aria-selected',on?'true':'false');}); ('.panel').forEach(p=>{const on=p.id===panel-; p.classList.toggle('show',on); if(on) p.removeAttribute('hidden'); else p.setAttribute('hidden','');}); } ('.tab').forEach(btn=>{ btn.addEventListener('click',()=>activate(btn.dataset.tab));}); activate('snake');})();
